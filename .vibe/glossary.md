# Ubiquitous Language

No domain vocabulary yet — only the app scaffold (`src/main.ts`, `src/version.ts`) exists so far. This file will be populated as lifebar parsing/editing concepts (elements, sprite assignment, etc.) land in code.
