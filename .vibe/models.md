# Data models

No data models yet — only the app version constant (`src/version.ts`) exists at this scaffold stage. This file will be populated once the lifebar `.def`-style parser (backlog item 002) introduces its data model.
